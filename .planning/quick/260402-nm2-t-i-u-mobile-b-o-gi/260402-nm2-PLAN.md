---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/quotation/page.tsx
  - components/quotation/MobileFooterSummary.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Mobile footer summary does not overlap with bottom navigation
    - All action buttons (Add, Save, Excel, PDF) are visible and tappable
    - Total amount is visible above the action buttons
    - Safe area respected on notched devices
  artifacts:
    - path: components/quotation/MobileFooterSummary.tsx
      provides: Footer summary with correct spacing above bottom nav
    - path: app/quotation/page.tsx
      provides: Main layout with proper z-index layering
  key_links:
    - from: MobileFooterSummary
      to: bottom-nav
      via: margin-bottom spacing
      pattern: mb-* pb-*
---

<objective>
Optimize mobile quotation page layout to fix overlapping footer elements.
</objective>

<context>
@app/quotation/page.tsx
@components/quotation/MobileFooterSummary.tsx

Current issue: MobileFooterSummary (z-50) and bottom nav (z-40) both fixed at bottom-0, causing visual overlap. The footer summary also needs proper safe-area padding.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MobileFooterSummary spacing and safe area</name>
  <files>components/quotation/MobileFooterSummary.tsx</files>
  <action>
    Update MobileFooterSummary container:
    - Add pb-safe (padding-bottom safe area) via `pb-[calc(5rem+env(safe-area-inset-bottom))]` or use inline style for dynamic safe area
    - Add mb-20 (margin-bottom) to prevent overlap with fixed bottom nav
    - Remove fixed positioning since parent container (page.tsx line 407) already has fixed positioning with rounded-t-3xl
    - Ensure the internal p-4 padding is sufficient for touch targets

    The footer container in page.tsx (line 407) is: `fixed bottom-0 inset-x-0 z-50 bg-white rounded-t-3xl`
    The bottom nav (line 421) is: `fixed bottom-0 w-full z-40`

    Since footer is z-50 and nav is z-40, footer should appear above nav visually but needs spacing so footer content doesn't get hidden behind nav bar height.
  </action>
  <verify>
    <automated>grep -n "mb-\|pb-\|pb-\[" components/quotation/MobileFooterSummary.tsx</automated>
  </verify>
  <done>MobileFooterSummary has bottom margin to clear bottom navigation bar</done>
</task>

</tasks>

<verification>
Build the project to ensure no layout conflicts: `npm run build 2>&1 | head -30`
</verification>

<success_criteria>
- Mobile footer summary is visible above bottom navigation
- All 4 action buttons are easily tappable
- Total amount displays correctly
- No layout overflow on small screens
</success_criteria>

<output>
After completion, create `.planning/quick/260402-nm2-t-i-u-mobile-b-o-gi/260402-nm2-SUMMARY.md`
</output>
