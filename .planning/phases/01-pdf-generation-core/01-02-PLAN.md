---
phase: 01-pdf-generation-core
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - libs/quotation-pdf-generator.tsx
autonomous: true
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04
  - PDF-05
  - PDF-06
must_haves:
  truths:
    - "PDF document can be generated from quotation data"
    - "Document uses A4 page size with proper margins"
    - "Header displays company info and quote number"
    - "Footer displays creator and signature fields"
    - "Product table excludes image column"
    - "Column widths are percentage-based and sum to 100%"
  artifacts:
    - path: "libs/quotation-pdf-generator.tsx"
      provides: "PDF document structure and rendering logic"
      exports: ["generateQuotationPDF", "QuotationPDFDocument"]
      min_lines: 150
  key_links:
    - from: "libs/quotation-pdf-generator.tsx"
      to: "libs/fonts.ts"
      via: "Import ROBOTO_FONT_FAMILY"
      pattern: "import.*ROBOTO_FONT_FAMILY.*from.*fonts"
    - from: "libs/quotation-pdf-generator.tsx"
      to: "types/quotation.ts"
      via: "Import Quotation, QuotationItem types"
      pattern: "import.*Quotation.*from.*@/types/quotation"
---

<objective>
Implement the core PDF document structure with A4 page layout, header/footer sections, product table (without images), percentage-based column widths, and professional black-and-white styling.

Purpose: Create the foundational PDF generator that transforms quotation data into a print-ready document structure, addressing all core PDF formatting requirements (PDF-01 through PDF-06).
Output: libs/quotation-pdf-generator.tsx with QuotationPDFDocument component and generateQuotationPDF server-side render function
</objective>

<execution_context>
@/home/tuong/.claude/get-shit-done/workflows/execute-plan.md
@/home/tuong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/01-pdf-generation-core/01-CONTEXT.md
@.planning/01-pdf-generation-core/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-pdf-generation-core/01-01-PLAN.md

# Existing codebase patterns
@libs/quotation-generator.ts (Data structure, category grouping, subproduct handling)
@types/quotation.ts (Quotation, QuotationItem types)
@libs/fonts.ts (ROBOTO_FONT_FAMILY export from Plan 01-01)

# Interfaces from existing code
From types/quotation.ts:
export interface Quotation {
  id?: string;
  quote_number: string;
  customer_name?: string;
  customer_phone?: string;
  customer_model?: string;
  customer_address?: string;
  notes?: string;
  subtotal_software: number;
  subtotal_hardware: number;
  discount_amount: number;
  total_amount: number;
  discounts?: QuotationDiscount[];
  status: QuotationStatus;
  created_at?: string;
  updated_at?: string;
}

export interface QuotationItem {
  id?: string;
  quotation_id?: string;
  product_id?: string;
  name: string;
  description?: string;
  unit: string;
  quantity: number;
  unit_price: number;
  total_price: number;
  is_free?: boolean;
  row_number?: number;
  product_category?: ProductCategory;
  image_url?: string | null;
  image_mime_type?: string | null;
  is_subproduct?: boolean;
  parent_item_id?: string;
  subproduct_id?: string;
  indent_level?: number;
}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PDF document structure with A4 layout and Vietnamese font</name>
  <files>libs/quotation-pdf-generator.tsx</files>
  <action>
    1. Create libs/quotation-pdf-generator.tsx file
    2. Import from @react-pdf/renderer: Document, Page, Text, View, StyleSheet, renderToBuffer
    3. Import ROBOTO_FONT_FAMILY from @/libs/fonts
    4. Import types: Quotation, QuotationItem from @/types/quotation
    5. Create StyleSheet with A4 page configuration:
       - page: size='A4', padding='15mm', fontFamily='Roboto', fontSize=10
       - header: marginBottom=10, borderBottomWidth=1, borderBottomColor='#000'
       - footer: marginTop=10, borderTopWidth=1, borderTopColor='#000'
       - categoryHeader: fontWeight='bold', fontSize=12, marginTop=5, marginBottom=5
       - table: width='100%'
       - tableRow: flexDirection='row', borderBottomWidth=1, borderBottomColor='#ccc'
    6. Create QuotationPDFDocument component that wraps content in Document and Page
    7. Use ROBOTO_FONT_FAMILY for all text styles
  </action>
  <verify>grep -q "Document\|Page" libs/quotation-pdf-generator.tsx && grep -q "Roboto" libs/quotation-pdf-generator.tsx</verify>
  <done>
    - QuotationPDFDocument component created
    - A4 page size configured with 15mm margins
    - Roboto font family applied to page styles
    - StyleSheet object with proper structure
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement header section with quote information</name>
  <files>libs/quotation-pdf-generator.tsx</files>
  <action>
    1. Add Header component to QuotationPDFDocument (inside Page, before main content)
    2. Add `fixed` prop to Header for multi-page repetition
    3. Include in header:
       - Bold title: "BAO GIA #{quotation.quote_number}"
       - Date: "Ngay: {new Date().toLocaleDateString('vi-VN')}"
       - Customer name if exists: "Khach hang: {quotation.customer_name}"
       - Customer phone if exists: "SĐT: {quotation.customer_phone}"
    4. Apply header style with bottom border for separation
    5. Use Text components with appropriate font sizes (14px for title, 10px for details)
  </action>
  <verify>grep -q "BAO GIA" libs/quotation-pdf-generator.tsx && grep -q "quote_number" libs/quotation-pdf-generator.tsx</verify>
  <done>
    - Header displays quote number with "BAO GIA" prefix
    - Header includes date in Vietnamese format
    - Header includes customer name when available
    - Header has bottom border for visual separation
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement product table without image column with percentage-based widths</name>
  <files>libs/quotation-pdf-generator.tsx</files>
  <action>
    1. Group items by category (software/hardware) using isSoftwareItem helper function
    2. Filter out subproducts from parent arrays (exclude where is_subproduct=true)
    3. Create table header row with column widths as percentages:
       - colName: width='35%' (Ten san pham)
       - colDescription: width='35%' (Mo ta)
       - colUnit: width='10%' (Don vi)
       - colQuantity: width='10%' (SL)
       - colPrice: width='10%' (Don gia)
       - colTotal: width='10%' (Thanh tien)
       - Total: 100% (verify no image column)
    4. Map over items and render table rows with:
       - item.name in Name column
       - item.description in Description column (use description || '')
       - item.unit in Unit column
       - item.quantity in Quantity column (format: toLocaleString('vi-VN'))
       - item.unit_price in Price column (format: toLocaleString('vi-VN'))
       - item.total_price in Total column (format: toLocaleString('vi-VN'))
       - "Miễn phí" text if is_free=true or unit_price=0
    5. Add "A. PHẦN MỀM" category header for software items
    6. Add "B. PHẦN CỨNG" category header for hardware items
    7. Apply tableRow style with bottom border
  </action>
  <verify>grep -q "colName.*35" libs/quotation-pdf-generator.tsx && grep -q "colDescription.*35" libs/quotation-pdf-generator.tsx && grep -q "colUnit.*10" libs/quotation-pdf-generator.tsx</verify>
  <done>
    - Table has 6 columns (no image column)
    - Column widths sum to 100% (35+35+10+10+10+10)
    - Software items grouped under "A. PHẦN MỀM"
    - Hardware items grouped under "B. PHẦN CỨNG"
    - Prices formatted with Vietnamese locale
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement footer section with creator and signature</name>
  <files>libs/quotation-pdf-generator.tsx</files>
  <action>
    1. Add Footer component to QuotationPDFDocument (inside Page, after main content)
    2. Add `fixed` prop to Footer for multi-page repetition
    3. Include in footer:
       - "Nguoi tao: ___________________" (underline with spaces)
       - "Chu ky: ___________________" (underline with spaces)
       - Page number: "Trang X / Y" using render prop ({pageNumber, totalPages})
    4. Apply footer style with top border for separation
    5. Use Text components with 10px font size
    6. Position creator on left side, signature on right side
  </action>
  <verify>grep -q "Nguoi tao" libs/quotation-pdf-generator.tsx && grep -q "Chu ky" libs/quotation-pdf-generator.tsx</verify>
  <done>
    - Footer displays "Nguoi tao" with underline space
    - Footer displays "Chu ky" with underline space
    - Footer shows page numbers (Trang X / Y)
    - Footer has top border for visual separation
  </done>
</task>

<task type="auto">
  <name>Task 5: Implement generateQuotationPDF server-side render function</name>
  <files>libs/quotation-pdf-generator.tsx</files>
  <action>
    1. Create generateQuotationPDF function (not a component)
    2. Accept parameters: quotation: Quotation, items: QuotationItem[]
    3. Return type: Promise<Buffer>
    4. Import renderToBuffer from @react-pdf/renderer
    5. Call renderToBuffer with QuotationPDFDocument component:
       ```typescript
       export async function generateQuotationPDF(
         quotation: Quotation,
         items: QuotationItem[]
       ): Promise<Buffer> {
         const pdf = await renderToBuffer(
           <QuotationPDFDocument quotation={quotation} items={items} />
         );
         return pdf;
       }
       ```
    6. Export generateQuotationPDF as named export
    7. Export QuotationPDFDocument as named export for testing
  </action>
  <verify>grep -q "export.*function.*generateQuotationPDF" libs/quotation-pdf-generator.tsx && grep -q "renderToBuffer" libs/quotation-pdf-generator.tsx</verify>
  <done>
    - generateQuotationPDF function exists and is exported
    - Function accepts quotation and items parameters
    - Function returns Promise<Buffer>
    - Function calls renderToBuffer with QuotationPDFDocument component
  </done>
</task>

</tasks>

<verification>
1. Run `node -e "const g = require('./libs/quotation-pdf-generator.tsx'); console.log('Module loads');"` to verify no syntax errors
2. Check that all required exports exist: `grep -E "(export.*generateQuotationPDF|export.*QuotationPDFDocument)" libs/quotation-pdf-generator.tsx`
3. Verify font family is imported: `grep -q "ROBOTO_FONT_FAMILY" libs/quotation-pdf-generator.tsx`
4. Check column widths sum to 100% (no image column)
</verification>

<success_criteria>
- PDF document structure created with A4 page size
- Header displays quote number, date, and customer info
- Table displays product items without image column
- Column widths are percentage-based and span full page width
- Footer displays creator, signature, and page numbers
- Server-side render function generateQuotationPDF exports successfully
- All text uses Roboto font for Vietnamese character support
</success_criteria>

<output>
After completion, create `.planning/phases/01-pdf-generation-core/01-02-SUMMARY.md`
</output>
